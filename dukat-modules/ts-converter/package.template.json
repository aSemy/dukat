{
  "name": "dukat-ts-distribution",
  "private": true,
  "version": "0.0.0",
  "description": "This is non-public package the only purpose of which is to build ts-related part of dukat project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "typescript": "${TS_VERSION}",
    "google-protobuf": "${PROTOBUF_VERSION}",
    "webpack": "5.75.0",
    "webpack-cli": "5.0.1",
    "@types/node": "12.7.1"
  },
  "license": "UNLICENSED"
}
